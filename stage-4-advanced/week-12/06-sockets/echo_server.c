#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <arpa/inet.h>

#define PORT 9000
#define BUF  1024

int main(void) {
    /* TODO: create TCP socket, bind to PORT, listen, accept one client,
             echo back everything it sends until client disconnects      */
    return 0;
}
