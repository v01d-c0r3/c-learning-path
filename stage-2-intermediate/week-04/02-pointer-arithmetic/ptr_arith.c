#include <stdio.h>

void print_array(const int *p, int len) {
    /* TODO: print each element using pointer arithmetic, no [] allowed */
}

int main(void) {
    int nums[] = {10, 20, 30, 40, 50};
    print_array(nums, 5);

    /* TODO: find the element 30 using a pointer, print its offset */
    return 0;
}
