#include <stdio.h>
#include <errno.h>
#include <string.h>

int main(void) {
    /* TODO 1: Try to open a non-existent file, print the error with perror */
    /* TODO 2: Try to open /etc/shadow, observe the errno value             */
    /* TODO 3: Write a safe_malloc that calls perror+exit on failure        */
    return 0;
}
