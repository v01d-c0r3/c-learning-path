#include <stdio.h>

typedef enum { MON=1, TUE, WED, THU, FRI, SAT, SUN } Weekday;

const char *day_name(Weekday d) {
    /* TODO: return the string name for each day */
    return "unknown";
}

int main(void) {
    /* TODO: print all day names using a loop */
    return 0;
}
